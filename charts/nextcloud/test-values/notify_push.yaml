fullnameOverride: nextcloud

nextcloud:
  host: "nextcloud-notify-push.nextcloud.svc.cluster.local"

redis:
  enabled: true

internalDatabase:
  enabled: false
mariadb:
  enabled: true

notifyPush:
  enabled: true
  autoSetup: true
